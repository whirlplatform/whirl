build:
  nodes:
    builder:
      environment:
        java: java-8-oracle
      dependencies:
        override:
          - command: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
            idle_timeout: 700