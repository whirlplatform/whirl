build:
  nodes:
    builder:
      environment:
        java: java-8-oracle
        variables:
          _JAVA_OPTIONS: '-Xms512m -Xmx6144m'
      dependencies:
        override:
          -
            command: mvn install -DskipTests=true
            idle_timeout: 300