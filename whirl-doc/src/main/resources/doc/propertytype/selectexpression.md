Sql-запрос, определяющий, какая строчка в дереве будет выделена. Одновременно может быть выделена только одна строчка. Если в запросе прописаны потенциально несколько строчек, то выделена будет последняя.

Пример запроса:
id::int in (2)
